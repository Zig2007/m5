const greet = function(name) { //atribui à constante greet uma function expression recebendo name.
  return "Olá, " + name + "!"; //constrói e retorna a saudação.
};

console.log(greet("Enzo")); //chama greet e imprime o resultado
